# 開発ルール

## ブランチ

- `main`: 安定版
- `develop`: 開発統合用
- `feature/xxx`: 機能開発用
- `fix/xxx`: 不具合修正用
- `docs/xxx`: ドキュメント整備用
- `refactor/xxx`: 挙動を変えない整理用
- `test/xxx`: テスト追加・修正用
- `chore/xxx`: 設定・保守作業用
- `ci/xxx`: CI変更用

`main`、`develop`へ直接コミット・直接Pushしないでください。
人間の開発者とCodexなどのAIコーディングエージェントは、原則として最新の`develop`から作業内容ごとのブランチを作成します。

詳しい命名規則、コミットの分け方、Push前チェックは `docs/git-workflow.md` を参照してください。

## 作業の流れ

1. Issueを作る
2. 最新の`develop`から作業内容に合ったブランチを作る
3. 実装する
4. 作業内容ごとにコミットを分ける
5. 作業ブランチをPushする
6. `develop`向けのPRを出す
7. レビュー後に`develop`へマージする

別の目的の変更が必要になった場合は、同じブランチやコミットへ混ぜず、別Issue・別ブランチとして分けてください。

## コミットメッセージ

既存ルールを維持し、次の形式を使用します。

```text
prefix: 内容を日本語で記述
```

例:

- `feat: クエスト達成APIを追加`
- `fix: クエストの重複受注を防止`
- `docs: Git運用ルールを追加`

## PRに書くこと

- 変更内容
- 変更した主なファイル
- 確認方法と実行したコマンド
- 実行できなかった確認と理由
- ドキュメント等に更新がある場合はその内容
