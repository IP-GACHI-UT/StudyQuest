import Card from "./Card";

export default function QuestCard() {
    return (
        <Card title="Available Quests">
            <p className="text-white">No quests available at the moment. Check back later for new challenges!</p>
        </Card>
    );
}